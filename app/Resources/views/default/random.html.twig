{% extends 'base.html.twig' %}

{% block title %}
  Fortunes :: La + random
{% endblock %}

{% block body %}
<div id="wrapper">
  <div id="container">
    <div id="status">
      <div class="section-title">
        Cette quote a été choisie par une licorne.
      </div>

      {% for story in stories %}
        {{ include("default/_fortune.html.twig") }}
        {{ include("default/_comments.html.twig") }}
      {% endfor %}

    </div>
  </div>
</div>
{% endblock %}
